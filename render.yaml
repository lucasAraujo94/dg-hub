services:
  - type: web
    name: dg-hub
    env: node
    buildCommand: >
      corepack enable || true;
      npm install -g pnpm@10.4.1;
      rm -rf dist .vite;
      pnpm install --frozen-lockfile;
      pnpm run build
    startCommand: pnpm start
